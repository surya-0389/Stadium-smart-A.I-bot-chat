services:
  - type: web
    name: stadium-ai
    runtime: node
    plan: free
    buildCommand: npm install --prefix backend
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: NODE_VERSION
        value: "20.19.0"
      - key: JWT_SECRET
        generateValue: true
