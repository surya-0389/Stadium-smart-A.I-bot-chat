services:
  - type: web
    name: stadium-ai
    runtime: node
    plan: free
    buildCommand: npm install --prefix backend
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: JWT_SECRET
        generateValue: true
